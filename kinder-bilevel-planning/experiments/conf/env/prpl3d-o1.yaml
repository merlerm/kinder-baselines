env_name: "prpl3d"

make_kwargs:
  id: "kinder/PrplLab3D-o1-v0"

env_model_kwargs:
  num_objects: 1

max_eval_steps: 500

# Bounded base actions need more simulation steps per skill than the global default.
max_skill_horizon: 1000
